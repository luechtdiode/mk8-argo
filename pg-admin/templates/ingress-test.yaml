kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: pg-admin-route-localtls
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  rules:
    - host: pg-admin.mars.interpolar.ch
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pg-admin
                port:
                  name: http
    - host: pg-admin.mws.interpolar.ch
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pg-admin
                port:
                  name: http
